import * as React from 'react';
import { IMultiBookProps } from './IMultiBookProps';

export default function IMultiBook(props: IMultiBookProps) {
    return (
        <div>Multiple Booking Test</div>
    )
}